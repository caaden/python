PAIR:

-conventional socket
    -strict 1 to 1
    -many to one: many clients to 1 server
    -one to many: multicast relationships

-exclusive pair pattern: similar to regular sockets
    -bidirectional communication
    -stateless
    -only one connected peer
    -server listens on a certain port, and client connects to it

